function main() {
    # This is a simple loop that counts from 1 to 15 
    Integer i = 0;
    while (i != 15) {
        i = i + 1;
        if (i > 5) {
            continue;
        }
        else {
            i = i + 1;
        }
        print i;
    }
}
